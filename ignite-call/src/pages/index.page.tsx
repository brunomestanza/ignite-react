// To have a better project folder config, we have a home folder inside of the pages folder.
// The route of the homepage is this file, because of that, it's the one with the page.tsx extension.
export { default } from './home'
